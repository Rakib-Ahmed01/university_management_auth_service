export type AcademicSemesterFilterOptions = {
  search: string;
  title: string;
  code: string;
  year: string;
};

export type AcademicFacultyFilterOptions = {
  title: string;
  search: string;
};
